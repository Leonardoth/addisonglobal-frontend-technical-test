import React from 'react';
import { Button, Container } from './Header.style';

function Header() {
  return (
    <Container>
      <Button>=</Button>
    </Container>
  );
}

export default Header;
